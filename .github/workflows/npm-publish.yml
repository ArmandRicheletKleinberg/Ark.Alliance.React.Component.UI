name: NPM Publish & Release

on:
  push:
    branches:
      - master
      - main
    tags:
      - 'v*'
  release:
    types: [created, published]
  workflow_dispatch:
    inputs:
      version_bump:
        description: 'Version bump type'
        required: false
        type: choice
        options:
          - patch
          - minor
          - major
        default: 'patch'
      publish_showcases:
        description: 'Also publish ShowCases to npm'
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  packages: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    outputs:
      package_name: ${{ steps.package.outputs.name }}
      package_version: ${{ steps.package.outputs.version }}
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'
          cache-dependency-path: './Ark.Alliance.React.Component.UI/package-lock.json'
      
      - name: Install Dependencies (UI Library)
        working-directory: ./Ark.Alliance.React.Component.UI
        run: npm ci
      
      - name: Run Build
        working-directory: ./Ark.Alliance.React.Component.UI
        run: npm run build
      
      - name: Run Tests (if available)
        working-directory: ./Ark.Alliance.React.Component.UI
        run: npm test --if-present || true
        
      - name: Pack Package
        working-directory: ./Ark.Alliance.React.Component.UI
        run: npm pack
      
      - name: Get Package Info
        id: package
        working-directory: ./Ark.Alliance.React.Component.UI
        run: |
          echo "name=$(node -p "require('./package.json').name")" >> $GITHUB_OUTPUT
          echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT
      
      - name: Upload Package Artifact
        uses: actions/upload-artifact@v4
        with:
          name: npm-package
          path: ./Ark.Alliance.React.Component.UI/*.tgz
          retention-days: 30
      
      - name: Create GitHub Release (if tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: ./Ark.Alliance.React.Component.UI/*.tgz
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Publish to NPM
        if: |
          (github.event_name == 'release' && github.event.action == 'published') ||
          startsWith(github.ref, 'refs/tags/v') ||
          github.event_name == 'workflow_dispatch'
        working-directory: ./Ark.Alliance.React.Component.UI
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      
      - name: UI Library Summary
        if: success()
        run: |
          echo "## 🎉 UI Library Published Successfully!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Package:** [${{ steps.package.outputs.name }}](https://www.npmjs.com/package/${{ steps.package.outputs.name }})" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** \`${{ steps.package.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Installation" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "npm install ${{ steps.package.outputs.name }}@${{ steps.package.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

  build-showcases:
    runs-on: ubuntu-latest
    needs: build-and-publish
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'
          cache-dependency-path: './Ark.Alliance.React.Component.Ui.ShowCases/package-lock.json'
      
      - name: Wait for NPM Registry Propagation
        run: |
          echo "Waiting for npm package to propagate..."
          sleep 30
          
      - name: Update UI Library Reference to NPM Version
        working-directory: ./Ark.Alliance.React.Component.Ui.ShowCases
        run: |
          echo "Updating ark-alliance-react-ui to version ${{ needs.build-and-publish.outputs.package_version }}"
          npm pkg set dependencies.ark-alliance-react-ui="^${{ needs.build-and-publish.outputs.package_version }}"
          cat package.json | grep ark-alliance-react-ui
      
      - name: Install Dependencies (ShowCases)
        working-directory: ./Ark.Alliance.React.Component.Ui.ShowCases
        run: npm install
      
      - name: Build ShowCases
        working-directory: ./Ark.Alliance.React.Component.Ui.ShowCases
        run: npm run build
      
      - name: Upload ShowCases Build
        uses: actions/upload-artifact@v4
        with:
          name: showcases-dist
          path: ./Ark.Alliance.React.Component.Ui.ShowCases/dist/
          retention-days: 30
      
      - name: Get ShowCases Package Info
        id: showcases_package
        working-directory: ./Ark.Alliance.React.Component.Ui.ShowCases
        run: |
          echo "name=$(node -p "require('./package.json').name")" >> $GITHUB_OUTPUT
          echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT
      
      - name: Publish ShowCases to NPM
        if: |
          (github.event_name == 'workflow_dispatch' && github.event.inputs.publish_showcases == 'true') ||
          startsWith(github.ref, 'refs/tags/v')
        working-directory: ./Ark.Alliance.React.Component.Ui.ShowCases
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      
      - name: ShowCases Summary
        if: success()
        run: |
          echo "## 📦 ShowCases Build Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Package:** ${{ steps.showcases_package.outputs.name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** \`${{ steps.showcases_package.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**UI Library Version:** \`${{ needs.build-and-publish.outputs.package_version }}\`" >> $GITHUB_STEP_SUMMARY

